Color Palette: Facebook Blue, clean whites and light grays
Typography: System fonts with weight variations for hierarchy
Shadows and Elevation: Cards have subtle elevation for depth
Rounded Corners: Consistent borderRadius throughout
FlatList: Used for infinite-scroll feed (HomeScreen, SearchScreen, etc.)
StyleSheet API: All styles use StyleSheet.create() — no inline styles

Screens

Home Feed — Stories + Create Post + Post Feed (FlatList)
Search — Search bar + People suggestions
Add Post — Text input + Media action grid
Notifications — Read/unread notification list
Profile — Cover photo, avatar, stats, user posts
